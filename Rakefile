require "rake"

namespace :ada do
  desc "Generate ADA embeddings for all text files in the examples directory"
  task :generate_embeddings do
    system("ruby scripts/generate_embeddings.rb")
  end
end
